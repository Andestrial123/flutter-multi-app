import 'package:flutter/material.dart';

class BakeryItemWidget extends StatelessWidget {
  const BakeryItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
